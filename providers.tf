terraform {
  required_version = ">= 1.5.0"

  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 3.0"
    }
  }
}

provider "azurerm" {
  # El proveedor azurerm requiere el bloque 'características', incluso si se deja vacío.
  features {}
}